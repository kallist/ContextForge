# ContextForge V0.2 release report

## Scope and decision

Release target: `@kallist/contextforge@0.2.0`, annotated tag `v0.2.0`, executable `contextforge`, Node `>=24.15 <25`, MIT. Release branch: `release/v0.2.0`. Synchronized starting main and V0.2-04 lineage: `0f864bd5fadd4990958b82613968c17f3c6960e7`. Starting worktree and main were clean. Base main Hosted CI [33848095818](https://github.com/kallist/ContextForge/actions/runs/33848095818) was successful.

Selected experimental candidate: **contextforge-v2-plan-pack**. Public/default Search and Pack: **V1**. Experimental exposure: **INTERNAL_ONLY**. A is ACCEPTABLE_EXPERIMENTAL, B is RETAIN_FOR_RESEARCH, C is ACCEPTABLE_EXPERIMENTAL and selected. There is no public strategy flag, MCP field or hidden environment switch.

V0.2 retains deterministic task analysis, hybrid candidate fusion and lexical ownership, bounded relationship intelligence and ContextPlan/Pack V2. Release finalization changes reporting, ADR status, version assertions and package/release checks only. Production source, ranking/packing policies, serializer, estimator, schema 2 and writer semantics are unchanged. API changes: none. DB changes: none. Providers and V0.3: NOT IMPLEMENTED by this task.

## Evidence record

The authoritative quality decision is [V0.2_FINAL_EVALUATION.md](V0.2_FINAL_EVALUATION.md). Raw data, all-budget aggregates, compact required-context diagnostics, 36 performance samples, failure matrix and integrity inventory are under `benchmarks/analysis/v0.2/final/`. The inventory binds the evidence to the evaluated source and frozen inputs; `scripts/release-evidence-check.mjs` verifies hashes, historical objects, aggregate means, 19/0, budget and performance gates. It does not run a new quality experiment or tune algorithms.

- Frozen dataset SHA-256: `75685087e8392840b4bb61ae19cef9ccb92df66844e79e0e87a0b7c6437a0002`.
- Four repositories, 24 tasks, budgets 2K/4K/8K/16K/32K, six systems, 720 cases.
- All 720 previous final case objects reproduced; previous 600 objects and 360 frozen V1 objects unchanged.
- Zero hard-budget violations. Each V2 candidate preserves all 19 established V1 successes, with zero new misses.
- Selected 8K required file/symbol: .916667/.886364; Overall Gold .899802; precision/noise .194804/.805196; mean payload 3656.833333.
- B/Pack V1 retains files 41/44 and symbols 35/39; C retains files 41/44 and symbols 36/39.
- Two full 24-task/three-variant diagnostic runs have identical SHA-256 `7a642da215cabf165ecd249f0f1c0e3a6b61be707cb820cb3990341911f63c4e`.
- Same-run fixed-corpus median C/V1 total: 1.478582, PASS against <=1.5, with limited headroom. Synthetic 1,000-file ratio: 1.084919. Windows x64 / Node 24.20.0, three repetitions. No SLA claim.

## Local qualification: TESTED

Commands executed on the unchanged evaluated base: `npm ci --no-audit --no-fund`, `npm run release:check`, `npm run benchmark`, two `node .benchmark-dist/benchmarks/src/cli.js diagnose-plan-pack` runs, `npm run benchmark:performance`, and network `npm audit --json --fetch-retries=0 --fetch-timeout=30000`.

The base release check composed lint, typecheck, 172 tests (172 passed, zero failures/skips), build, CLI smoke, package smoke, release hardening, benchmark validation and 24-case benchmark smoke. The first benchmark attempt failed before scoring because the sandbox denied a Git child process with EPERM; the authorized environment completed the single formal scoring run without code or parameter changes.

Installed base package qualification: 169 files/663137 packed bytes, five WASM assets; package identity remained 0.1.1 at that checkpoint. This is qualification of V0.2 implementation before the release version bump, not evidence that 0.2.0 was published. The 0.2.0 package receives its own final release checks.

Hardening: 1,200 generated files, 1,202 indexed files/symbols, 1,199 graph edges; 10 deterministic real-process Index/Index contention iterations, zero failures; forced-kill recovery, zero incomplete generations, SQLite integrity `ok`, zero foreign-key errors; 1,000-request MCP soak, zero orphan processes and handles 199 -> 199. Corrupt/future schema and missing-parser cases returned the expected bounded errors. The earlier V0.2-04 Windows observation timeout did not recur in this qualification.

Base network npm audit: **PASS**, zero reported vulnerabilities across 101 dependencies. The dependency set is unchanged by the version bump, but final local 0.2.0 audit requests timed out at 30 seconds and at a single controlled 60-second recheck using a writable cache. Final local audit status: **NOT VERIFIED**. No further local retries are used. A separate `npm audit --json` step in Hosted CI must pass before merge/publication; a timeout is never reported as audit success.

### Final 0.2.0 local validation

Clean `npm ci --no-audit --no-fund` installed 101 packages. `npm run release:check` passed: lint, typecheck, 172/172 tests (zero skips), build, CLI smoke, fresh local-tarball parser/SQLite/CLI/MCP smoke, release hardening, frozen validation and 24-case benchmark smoke. After final review edits, lint and the release-evidence checker passed again. The source and evaluator fingerprints remain unchanged.

The 0.2.0 reviewed tarball has 169 files, 663755 packed bytes and 4840887 unpacked bytes, with five WASM assets and no detected private metadata. Hardening again completed 10 contention iterations without failure and 1,000 MCP requests with zero orphans and handles 199 -> 199. This validates the local tarball, not public publication. The added public-registry smoke branch is NOT TESTED until the package is published.

## Security and boundaries

Ignore/sensitive/dependency/generated policy, repository/symlink/junction checks, source SHA verification and generation isolation remain unchanged. Core operation does not execute corpus source, call a model or access a network. SQLite stores metadata/ranges rather than source bodies. Relationship data and request-local fragments remain transient and bounded. Committed diagnostics contain identities, ranges and hashes, with no source bodies, credentials or private paths. Package smoke scans actual installed contents and checks WASM assets, install hooks and private metadata.

Public V1 real-process hardening and V2 application/benchmark tests are separate evidence. There is no PostgreSQL adapter or new migration to claim as tested.

## Release wiring and publication gates

`package.json` and its lockfile root version are 0.2.0. CLI and hardening assertions are updated to the exact release identity. Package smoke explicitly verifies installed CLI/MCP version and V1 defaults. Its `--public-registry` mode installs `@kallist/contextforge@0.2.0` from `https://registry.npmjs.org` into a fresh temporary project and fresh cache, then runs the same parser/SQLite/CLI/official-client MCP flow. This mode must only be called after publication. Normal smoke continues to install the reviewed local tarball.

The source release report is a pre-publication record; it does not assert future external gates. Publication is complete only when all of the following are recorded in the release closeout:

1. Final local 0.2.0 checks and package-content review pass.
2. Draft release PR receives a clean final review and all five final-head Hosted CI jobs pass.
3. PR merges with a merge commit, synchronized clean main and all five post-main jobs pass.
4. `release:check` and package checks pass on that main; annotated v0.2.0 points exactly to it.
5. Public npm publication and registry version/latest/integrity are verified.
6. Fresh public-registry CLI/MCP smoke passes with V1 defaults.
7. GitHub Release is published as latest, with exact tag/npm/install evidence and truthful notes.

The [v0.2.0 GitHub Release](https://github.com/kallist/ContextForge/releases/tag/v0.2.0), once published, carries the final closeout evidence and immutable merge/tag/package identities. The [public npm version](https://www.npmjs.com/package/@kallist/contextforge/v/0.2.0) must independently exist. Neither a local tarball nor this document establishes publication. Never move the tag or republish an existing version after a partial failure.

## Independent review and ADRs

Review covers candidate selection across every budget, historic object identity, source/default invariance, package files, source-free/path-safe evidence, public assertions, version wiring and honest claims. Required release disposition: BLOCKING = 0 and IMPORTANT = 0 unresolved; actual final review and CI status belong to the release closeout.

ADR-009: Accepted as maintained experimental fusion/planning architecture shared by all V2 candidates. ADR-010: Accepted as bounded transient relationship architecture used by C, even though standalone B is retained for research. ADR-011: Accepted as maintained experimental Pack V2 after exact-output and hard-budget/performance checks. None authorizes a public-default switch or a universal superiority claim.

## Important limitations / NOT TESTED

Cross-budget file/symbol/precision regressions remain. PACK_VALUE_MIXED and RELATIONSHIP_VALUE_MIXED remain. Task-aware planning is NOT PROVEN STABLY SUPERIOR. Strict .917 file and .900 symbol targets are NOT MET; the other stated 8K targets pass. Stale source, index activation, parser assets, snapshot architecture and output publication cases remain individually documented.

Docker, actual third-party coding-agent hosts, coding-agent success, very-large external repository quality, Pack V2-specific OS-process symlink/concurrency races, cloud and multi-repository behavior: **NOT TESTED**. No agent success, 60% savings, universal lexical/V1 superiority, complete call graph, sound whole-program impact or optimal packing claim is permitted.

Historical unsuccessful 02R and 04/04R experiments and subsequent correctness/performance closeouts remain available in phase documents. Known quality limitations are research records, not permission to start new algorithms. **V0.3 NOT STARTED.**
