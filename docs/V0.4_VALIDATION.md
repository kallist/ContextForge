# V0.4 validation record

Starting main: `c51a7d3ac4e92d44ea7fe90df5f09f6dedf42af4` (v0.3.0).
Implementation branch: `codex/v0.4-review`. Local platform: Windows x64,
Node 24.20.0, Chromium 153.0.8010.12. Hosted CI and publication remain separate
gates; local evidence alone is not a release claim.

## Executed locally

- Lint and TypeScript typecheck passed.
- Full regression suite: 207 passed, 0 failed/skipped before the final filter
  hardening test was added. The final targeted Review suite then passed 4/4,
  including malicious Git clean/process filters. Hosted CI runs all tests together.
- Review tests use real Git repositories and cover changed symbols, direct
  references/tests/implementations, add/modify/delete/rename, ignored/sensitive and
  binary paths, deletion-only ranges, removed untracked files, malicious refs,
  malformed Capsules, no changes, immutable controls, history and exact replay.
- Real Chromium: existing Studio lifecycle plus Review budget loss → PIN →
  recompile → semantic Diff → exact Replay. Source and stored-metadata XSS,
  ref rejection, privacy and widths 1024/1280/1512/1920 passed.
- Fresh local-tarball installation: CLI, MCP, parser WASM, production Studio assets,
  existing and Review browser journeys passed. A separate installed Review CLI
  command chain verifies Capsule/source outputs, Explain, Coverage, PIN, Diff and
  Replay. Package leakage checks passed.
- Release hardening passed: OS-process concurrency/recovery and 1,000-request MCP
  soak; zero orphan processes and stable handle count were recorded.
- Historical benchmark: **720/720 unchanged case objects**. Frozen source/artifact
  integrity checks passed without exemptions or Gold/metric changes.
- Capsule conformance: **240/240**, aggregate SHA-256
  `bc0429b0c9a583e202c762935a5d8ab35619e05374fa58a24c988abef760602f`.
- Separate Review evaluation: six cases, exact repetition and hard-budget checks;
  see raw results and the explicit caller retention losses in the evaluation report.

## Final self-review

The final review pass examined Git argument construction, external filters, source
verification, bounds, Capsule version/hash compatibility, history transactions,
immutable control provenance, UI rendering, package paths, frozen composition and
claims. Findings fixed: missing score attribution, rename old-path index signals,
removed untracked-path signals, deletion-only symbol intersection, stale v0.3
version assertion, and executable Git filters. No unresolved BLOCKING or IMPORTANT
finding remained in that pass. This was a separate self-review pass, not an
external independent human audit.

The first hosted macOS package smoke exposed a fixture-path issue: macOS's
temporary directory alias traversed a symlink rejected by the existing history
store guard. Review CLI/browser/demo scripts now canonicalize their own temporary
root, following the existing package/Studio smoke convention. The store guard and
all behavioral assertions remain unchanged; the corrected macOS gate must pass.

## Limits

No external provider or coding/review agent was called. **CODING/REVIEW AGENT
SUCCESS NOT MEASURED.** Docker, runtime/whole-program impact, large-project
performance, concurrent mutation of host Git configuration, and external human
review quality were not tested. Deleted source is metadata only; custom Git clean
filters are disabled; submodule contents are not traversed. Review input is
base-to-tracked-working-tree, not arbitrary historical/staged-only patch analysis.

Index schema 2, history storage schema, ordinary V1 compilation, public MCP tools,
and frozen algorithm/evidence files are unchanged. New readers accept v1 and v2
Capsules; older readers explicitly reject Review v2.
