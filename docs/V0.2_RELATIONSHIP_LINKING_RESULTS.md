# ContextForge V0.2-03 Relationship Linking Results

## Status

**IMPLEMENTED AND TESTED AS A BENCHMARK-SELECTABLE EXPERIMENT — MEASURED RESULT: MIXED**

Implementation commit: `51bb58e2a7886b7977208db855b3481588b578e0`.

Local benchmark environment: Windows x64 `10.0.26200`, Node `v24.19.0`. Quality and relationship-diagnostic evidence is deterministic where the benchmark promises deterministic fields. Timing is environment-specific and is not an SLA.

## 1. Goal

Test whether bounded, high-confidence symbol, caller, implementation, and test relationships add retrieval value on top of the V0.2-02 foundation without changing Pack V1, the SQLite schema, security exclusions, or the public V1 default.

## 2. Scope

This slice adds an internal `contextforge-retrieval-v2-relations` comparison that uses the same TaskAnalysis, ContextPlan, CandidateEvidence, Candidate Fusion, Ranking V2, verified source snapshot, and Pack V1 path as V0.2-02. It adds no public CLI flag or MCP surface. It adds no dependency, database migration, network/model call, persisted source body, or source execution.

## 3. Relationship Model

`RelationshipEvidenceV2` records one directional repository-relative relationship with a stable ID, typed source and target file/symbol identities, coarse confidence, explicit classification, derivation, generation-bound provenance, optional source location, diagnostics, and fixed distance `1`. The contract rejects absolute/traversing paths and invalid fact/confidence combinations. Repeated call sites for one semantic source/type/target relationship retain the deterministic earliest provenance location and do not multiply score.

Implemented relationship types are `SYMBOL_REFERENCES_SYMBOL`, `SYMBOL_IMPLEMENTS_SYMBOL`, `TEST_REFERENCES_SYMBOL`, `TEST_IMPORTS_FILE`, `TEST_ASSOCIATED_WITH_FILE`, and `FILE_DEPENDS_ON_FILE`. Existing generation-graph import/test facts are reused and exposed through the contract; they are not persisted again or independently double-scored.

## 4. Fact vs Heuristic

- `STRUCTURAL_FACT` requires `EXACT` confidence. It covers unique parsed direct calls, uniquely resolved named/namespace imports, explicit uniquely resolved TypeScript `implements`, direct test symbol calls, and existing exact import/test graph edges.
- `HEURISTIC` cannot use `EXACT`. Existing unique-basename/mirrored-directory test associations retain `HIGH` or `MEDIUM` confidence and their original graph derivation.
- No unresolved textual call-like match becomes a relationship. Frozen full diagnostics report 1,527 visible relationships: 1,521 structural facts and 6 explicit heuristics. Heuristics produced zero relationship-only candidate discoveries.

## 5. Caller Linking

Supported caller facts are same-file unique top-level identifier calls, same-class `this`/`self` method calls, calls through one uniquely resolved named import, and calls through one uniquely resolved namespace import. The general stale-source case derives `FileSystemRepositorySourceReader.readTextFile -> readOnce` from parsed same-file syntax with exact generation/location provenance.

Dynamic receiver dispatch, reflection, function values, prototype mutation, monkey patching, ambiguous imports, duplicate symbol targets, and alias chains that cannot be resolved uniquely emit no caller edge. Partial/degraded parse results fail closed.

## 6. Interface / Implementation

TypeScript/TSX explicit `class X implements Y` is supported when `Y` is a unique same-file interface or a named import whose module and interface resolve uniquely. Reverse discovery reuses the same directional fact. JavaScript/Python type-shape or same-method-name inference is not implemented. Extends/inheritance, generics requiring compiler resolution, qualified type expressions, declaration merging, and ambiguous imports are rejected rather than guessed.

## 7. Test Linking

A parsed call from a test symbol to a uniquely resolved production symbol produces `TEST_REFERENCES_SYMBOL`. Existing graph facts distinguish direct test imports (`TEST_IMPORTS_FILE`, structural) from naming/directory association (`TEST_ASSOCIATED_WITH_FILE`, heuristic). The wording is deliberately `references`, `imports`, or `associated with`; runtime coverage is not measured and `TEST_COVERS` is not claimed.

## 8. Impact Bounds

Impact means direct nearby repository entities, not definite runtime impact. Expansion starts from at most 24 priority-tier-1/2 candidates and 24 seed symbols, parses at most 128 verified files that are seeds or actually spell a seed symbol, walks only distance `1`, accepts at most 16 relationships per seed, 8 callers, 8 tests, 8 implementations, and 256 total relationship evidence items, and keeps the existing final 256-candidate cap. Degree above 24 is damped; cap and hub events are reported. No recursive closure is performed, so cycles cannot loop.

## 9. Security

Relationship derivation receives only files admitted by the Safe Repository Map and re-read with the active generation hash. Candidate Fusion rejects targets outside that snapshot. A relation cannot reintroduce ignored, sensitive, generated-policy, dependency, symlink/junction escape, or outside-root files. The committed model persists no source body, secret, credential, task, absolute private path, or relationship row. It executes no repository code, shell, model, or network request.

## 10. Performance

Three environment-specific repetitions on implementation commit `51bb58e` stayed below the proposed `1.5x` warm Search and Pack guardrail relative to V1 on both corpora.

| Corpus | V1 Search | V0.2-02 Search | V0.2-03 Search | V0.2-03/V1 | V1 Pack | V0.2-03 Pack | Ratio | V1 total | V0.2-03 total | Ratio |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `contextforge-phase5` | 564.719 ms | 556.090 ms | 772.592 ms | 1.368x | 181.151 ms | 172.676 ms | 0.953x | 738.205 ms | 939.166 ms | 1.272x |
| `medium-synthetic-1000` | 2662.497 ms | 2746.439 ms | 2782.515 ms | 1.045x | 69.392 ms | 77.534 ms | 1.117x | 2731.889 ms | 2860.048 ms | 1.047x |

Median relationship derivation/expansion was 201.335/3.286 ms on `contextforge-phase5` and 11.626/0.428 ms on the synthetic corpus. Timing artifact SHA-256 is `5f509bda0927e11e805ead2f3a848353ae783e9fdc25d4376da79dd7e93856a6`; timing is excluded from deterministic quality claims.

## 11. Benchmark Protocol

- benchmark: `contextforge-benchmark-v1`;
- evaluation addition: `contextforge-v0.2-03-evaluation-v1`;
- dataset: `contextforge-dataset-v1`, 24 tasks, 4 repositories, 5 budgets;
- dataset SHA-256: `75685087e8392840b4bb61ae19cef9ccb92df66844e79e0e87a0b7c6437a0002`;
- systems: `lexical-full-file-v1`, `structural-full-file-v1`, `contextforge-v1`, `contextforge-v2`, and `contextforge-v2-relations`;
- formal cases: 600; V1 cases and V0.2-02 comparison remain present;
- packing/estimation: unchanged `contextforge-pack-v1` and `contextforge-generic-v1` 1.0;
- Gold, tasks, repositories, budgets, metric definitions, and reference artifacts: unchanged.

Frozen V1 artifact hashes were revalidated as `175ec7327dba2880324dad56d8d055fba2f09992a957bfe754607c6d9c1394a6` (quality), `a3cd681db283fd7a34522f7254247ffd4320abefb54ab6cae5ee4918bf4915a3` (aggregate), `e6283faeb037f39151351b0b1abdbf054385dbeccf46fce9a41afa3f1f070023` (report), and `f8933cda8eb48077b74cb65ee30ba8bd155ff92cc51bbb3aeac824098aec858d` (performance). Final V0.2-03 quality artifacts are `c3504a8756d77cf102b46e7a6094e7027be4d5b53c723c348532a7cc06087713`, `de10cec7adb75515fc6cade119227402c63486a2b037c09b41833158d10d6a00`, and `08f111ef76e22aa16e4374c8804b8683d7df8daf37c67d6d7b796af643beff44` for quality, aggregate, and report respectively.

## 12. Ablations

The bounded 8K sequence holds V0.2-02 weights fixed and only enables cumulative relationship families.

| Variant | Required file | Required symbol | Overall Gold | Precision | Scored relation evidence | Relationship-only | Callers | Tests | Implementations |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| foundation | .917 | .864 | .882 | .196758 | 0 | 0 | 0 | 0 | 0 |
| + references | .917 | .864 | .882 | .196762 | 537 | 11 | 0 | 0 | 0 |
| + callers | .917 | .864 | .871 | .194644 | 617 | 11 | 93 | 0 | 0 |
| + interface/implementation | .917 | .864 | .871 | .194644 | 625 | 11 | 93 | 0 | 8 |
| + tests | .917 | .864 | .871 | .194651 | 678 | 11 | 93 | 53 | 8 |
| full relationships | .917 | .864 | .871 | .194651 | 678 | 11 | 93 | 53 | 8 |

Direct file dependents already enter the unchanged structural expansion, so the final dependent toggle creates no second score for the same graph fact. Full diagnostics recorded 254 fanout-cap events and 102 hub-damping events. These are bounded truncation evidence, not hidden removals.

## 13. Results

### Retrieval recall before Pack

| System | File @1 | @5 | @20 | Symbol @1 | @5 | @20 |
|---|---:|---:|---:|---:|---:|---:|
| lexical-full-file-v1 | .479 | .896 | .944 | .437 | .731 | .731 |
| structural-full-file-v1 | .299 | .708 | .938 | .326 | .602 | .754 |
| contextforge-v1 | .299 | .708 | .938 | .326 | .602 | .754 |
| contextforge-v2 | .340 | .757 | .917 | .383 | .811 | .871 |
| contextforge-v2-relations | .340 | .757 | .917 | .383 | .811 | .871 |

### Fixed 8K quality

| System | Required file | Required symbol | Overall Gold | Precision | Noise | Mean payload |
|---|---:|---:|---:|---:|---:|---:|
| lexical-full-file-v1 | .819 | .848 | .826 | .173513 | .826487 | 3510.917 |
| structural-full-file-v1 | .750 | .773 | .756 | .138007 | .861993 | 3794.667 |
| contextforge-v1 | .917 | .864 | .871 | .192850 | .807150 | 3453.375 |
| contextforge-v2 | .917 | .864 | .882 | .196758 | .803242 | 3520.875 |
| contextforge-v2-relations | .917 | .864 | .871 | .194651 | .805349 | 3516.375 |

Relationship V2 versus foundation V2 is 2 wins, 21 ties, and 1 loss at 8K. Required recall is unchanged; overall Gold and precision are lower than V0.2-02 but remain marginally above V1. This does not support a general V2 or relationship-ranking superiority claim.

## 14. Failure Cases

| Task | Foundation -> relationships before Pack | Final Pack V1 result | Classification |
|---|---|---|---|
| `self-stale-lexical-source` | required files `4,22 -> 3,21`; required symbols `4 -> 3,21`; exact caller facts expose `readTextFile -> readOnce` | file recall `.5`, symbol recall `0`; both ranked required symbols are dropped by `SECTION_LIMIT` | **Retrieval relationship improved; Pack V1 residual** |
| `self-index-activation-race` | required file/symbol ranks remain `6,8` | file `.5`, symbol `0`; Pack drops both symbols | no relationship rank improvement |
| `self-package-parser-assets` | files remain `3,38`; generic `run` symbol remains unresolved | file `1`, symbol `0`; precision `.100816 -> .104300` | missing symbol unchanged; no forced relation |
| `self-pack-snapshot-architecture` | required document remains rank `43` | required file `0`; overall `.5 -> .25`, precision `.110098 -> .055957` | Pack/document residual; unfavorable relationship result retained |
| `self-output-publish-race` | required file/symbol remain rank `3` | full file/symbol/overall recall `1`; precision `.232840` unchanged | protected; no regression |

The stale-source relationship is general behavior-level evidence too: focused fixtures independently prove same-file caller, uniquely resolved imported caller, ambiguous dynamic-call rejection, interface/implementation both directions, production/test reference, hub cap, cycle termination, deduplication, and exclusion safety. No frozen task ID or fixture path appears in production code.

## 15. Success Regressions

The 19 known V1-success tasks were evaluated and produced zero new required-context misses. `self-output-publish-race` retained the V0.2-02 full-recall and precision result. This passes the protected-success gate but does not erase the single V2-relative loss in the complete 24-task comparison.

## 16. Limitations

- This is bounded relationship evidence, not a complete call graph or sound global impact analysis.
- Dynamic dispatch, reflection, function-value flows, TypeScript compiler semantics, LSP resolution, inheritance, advanced aliases, JavaScript prototype behavior, Python monkey patching/import hooks, and runtime test coverage are not implemented.
- TypeScript/JavaScript/Python have intentionally unequal reliable relationship support.
- Relationship syntax is derived transiently per request; the observed cost is bounded but visible.
- Existing graph test-name heuristics are exposed honestly but do not receive a second relationship score.
- Required-symbol recall remains `.864`, below the V0.2-wide `.900` design target.

## 17. Deferred to Pack V2

Pack V1 still drops ranked required context through fixed section limits in `self-stale-lexical-source`, `self-index-activation-race`, and other recorded required-symbol losses. `self-pack-snapshot-architecture` remains a document/section-allocation failure. V0.2-04 should consume these exact cases as plan-aware allocation inputs; this phase did not alter roles, shares, range budgets, section limits, or high-priority drop behavior.

## 18. Claims Allowed / Forbidden

Allowed by measured evidence:

- bounded exact caller evidence connected `readOnce` to `readTextFile` and improved their frozen pre-Pack ranks;
- the implementation preserved all 19 known V1 successes and the `self-output-publish-race` result;
- relationship expansion is deterministic, one-hop, capped, hub-damped, generation-bound, and security-filtered;
- the measured value classification is **RELATIONSHIP_VALUE_MIXED**.

Not supported:

- ContextForge V2 is generally better than V1 or lexical search;
- relationship-enabled V2 is generally better than foundation V2;
- ContextForge finds all callers, knows which tests cover every symbol, has a complete call graph, or performs sound impact analysis;
- the current timing is an SLA or the finite frozen benchmark proves external coding-agent success.
