openapi: 3.1.0
info:
  title: Profile API
  version: 1.0.0
paths:
  /profiles/{userId}:
    get:
      operationId: getProfile
    put:
      operationId: updateProfile
components:
  schemas:
    Profile:
      type: object
      required: [userId, displayName]
